const getters = {
    subContents: ({ subContents }) => subContents,
    subContent: ({ subContent }) => subContent,
    subContentsBySubject: ({ subContentsBySubject }) => subContentsBySubject,
};

export default getters;